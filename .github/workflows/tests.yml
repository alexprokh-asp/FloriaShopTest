name: API Tests (Newman)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  newman-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Create reports folder
        run: mkdir -p reports

      - name: Run Newman tests
        continue-on-error: true
        run: |
          DATE=$(date +'%Y-%m-%d_%H-%M-%S')
          npx newman run postman/collection.json \
            -r cli,htmlextra \
            --reporter-htmlextra-export "reports/report-$DATE.html"

      - name: Commit and Push Report to Repository
        run: |
          # 1. Configure local git profile
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 2. Stage and commit the new report file first
          git add reports/
          git commit -m "chore: add automated test report $(date +'%Y-%m-%d %H:%M:%S')"
          
          # 3. Pull latest changes with rebase to safely merge if needed
          git pull origin main --rebase
          
          # 4. Push the commit to your repository
          git push origin main
